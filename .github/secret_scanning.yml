# The Flutter web bundle is generated output and contains Firebase web config values.
# Firebase web apiKey is client-side by design, so this file is excluded to prevent repeated false-positive alerts.
paths-ignore:
  - "web/main.dart.js"